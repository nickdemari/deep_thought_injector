---
phase: 02-core-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/src/auto_wiring.dart
  - lib/src/deep_thought_config.dart
  - lib/src/deep_thought_injector.dart
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [CORE-03, CORE-04, CORE-05]

must_haves:
  truths:
    - "logging package is declared in pubspec.yaml and dart pub get resolves cleanly"
    - "AutoWiring class does not exist anywhere in the codebase"
    - "DeepThoughtConfig class does not exist anywhere in the codebase"
    - "dart analyze produces no errors related to missing imports or undefined classes"
  artifacts:
    - path: "pubspec.yaml"
      provides: "logging dependency declaration"
      contains: "logging: ^1.2.0"
    - path: "lib/src/deep_thought_injector.dart"
      provides: "Clean imports with no duplicates"
    - path: "CLAUDE.md"
      provides: "Architecture section without deleted class references"
    - path: "README.md"
      provides: "No DeepThoughtConfig code example"
  key_links:
    - from: "lib/src/deep_thought_injector.dart"
      to: "package:logging/logging.dart"
      via: "pubspec.yaml dependency declaration"
      pattern: "logging: \\^1\\.2\\.0"
---

<objective>
Declare the missing logging dependency, delete the AutoWiring and DeepThoughtConfig stub files, clean up all references to both classes, and fix the duplicate import in deep_thought_injector.dart.

Purpose: The codebase currently has an undeclared runtime dependency and two dead stub classes generating analyzer warnings. This plan eliminates all of that noise so the codebase is clean for new feature work.
Output: A codebase with zero references to deleted stubs, a properly declared logging dependency, and a cleaner analyzer output.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-cleanup/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/src/deep_thought_injector.dart (current state -- line 6 is the duplicate import to remove):
```dart
import 'package:deep_thought_injector/src/sub_etha_scope.dart';  // line 3 -- KEEP
import 'package:deep_thought_injector/src/vogon_poetry_exception.dart';  // line 4
import 'package:logging/logging.dart';  // line 5
import 'sub_etha_scope.dart'; // line 6 -- DELETE (duplicate_import)
```

From pubspec.yaml (current state -- NO dependencies section exists):
```yaml
name: deep_thought_injector
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
```

From README.md (lines 72-80 -- section to remove):
```markdown
### Configuration & Overrides

A basic configuration class is available:
\```dart
import 'package:deep_thought_injector/src/deep_thought_config.dart';

final config = DeepThoughtConfig(environment: 'staging');
// Extend this class to override dependencies per environment.
\```
```

From CLAUDE.md Architecture section (mentions to remove):
- "DeepThoughtConfig (lib/src/deep_thought_config.dart) -- Environment configuration holder"
- "AutoWiring (lib/src/auto_wiring.dart) -- Stub for automatic dependency registration"
- Barrel file description mentions "DeepThoughtConfig and AutoWiring are internal"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare logging dependency and delete stub files</name>
  <files>pubspec.yaml, lib/src/auto_wiring.dart, lib/src/deep_thought_config.dart</files>
  <action>
1. Edit pubspec.yaml: Add a `dependencies:` section above `dev_dependencies:` containing `logging: ^1.2.0`. This section does not exist yet -- create it.

2. Delete `lib/src/auto_wiring.dart` entirely.

3. Delete `lib/src/deep_thought_config.dart` entirely.

4. Run `dart pub get` to verify the logging dependency resolves.

5. Verify no source files (lib/ or test/) import either deleted file:
   - `grep -r "auto_wiring" lib/ test/` should return nothing
   - `grep -r "deep_thought_config" lib/ test/` should return nothing
   (The barrel file lib/deep_thought_injector.dart does NOT export either -- confirmed in research.)
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/learning/deep_thought_injector && dart pub get && test ! -f lib/src/auto_wiring.dart && test ! -f lib/src/deep_thought_config.dart && echo "PASS"</automated>
  </verify>
  <done>pubspec.yaml has logging: ^1.2.0 under dependencies, both stub files are deleted, dart pub get succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Clean up imports, documentation, and analyzer warnings</name>
  <files>lib/src/deep_thought_injector.dart, README.md, CLAUDE.md</files>
  <action>
1. Edit `lib/src/deep_thought_injector.dart`: Remove line 6 (`import 'sub_etha_scope.dart'; // now includes Lifecycle`) -- this is a duplicate of the package import on line 3. The `always_use_package_imports` lint rule means the package import is the correct one to keep.

2. Edit `README.md`: Remove the "Configuration & Overrides" section (lines 72-80). This section contains a code example importing and using `DeepThoughtConfig`, which no longer exists. Remove the heading, the descriptive text, and the entire code block.

3. Edit `CLAUDE.md` Architecture section:
   - Remove the bullet point about `DeepThoughtConfig` ("DeepThoughtConfig (`lib/src/deep_thought_config.dart`) -- Environment configuration holder...")
   - Remove the bullet point about `AutoWiring` ("AutoWiring (`lib/src/auto_wiring.dart`) -- Stub for automatic dependency registration...")
   - Update the barrel file description: Remove the sentence "`DeepThoughtConfig` and `AutoWiring` are internal (`lib/src/`) and not re-exported." since both classes no longer exist

4. Also remove the unused `_lock` field from `lib/src/sub_etha_scope.dart` line 36 (`final _lock = Object();`) -- it was flagged as `unused_field` by the analyzer and was left behind from Phase 1 cleanup. This is a one-line deletion in a file we're already verifying. (Note: add this file to the modified list.)

5. Run `dart analyze --fatal-infos` and verify zero new errors from these changes. Pre-existing warnings in untouched files are expected and out of scope.
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/learning/deep_thought_injector && dart analyze --fatal-infos 2>&1 | grep -c "duplicate_import\|depend_on_referenced_packages" | xargs test 0 -eq && echo "PASS"</automated>
  </verify>
  <done>No duplicate import warning, no depend_on_referenced_packages warning, README has no DeepThoughtConfig references, CLAUDE.md has no AutoWiring/DeepThoughtConfig bullets, _lock field removed from sub_etha_scope.dart</done>
</task>

</tasks>

<verification>
All phase cleanup verified by:
1. `dart pub get` succeeds (logging dependency resolves)
2. `dart analyze --fatal-infos` shows no errors related to deleted classes or missing dependencies
3. `grep -r "AutoWiring\|DeepThoughtConfig\|auto_wiring\|deep_thought_config" lib/ test/` returns nothing
4. `dart test` full suite passes (no regressions from cleanup)
</verification>

<success_criteria>
- pubspec.yaml declares `logging: ^1.2.0` under `dependencies:`
- lib/src/auto_wiring.dart does not exist
- lib/src/deep_thought_config.dart does not exist
- lib/src/deep_thought_injector.dart has no duplicate imports
- lib/src/sub_etha_scope.dart has no unused _lock field
- README.md has no Configuration & Overrides section
- CLAUDE.md Architecture section reflects only the three remaining classes
- `dart test` passes
- `dart analyze` produces no errors from these changes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-cleanup/02-01-SUMMARY.md`
</output>
